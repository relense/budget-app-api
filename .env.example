# Copy this file to .env and fill in real values. .env itself is gitignored.

NODE_ENV=development
PORT=4000

# Matches docker-compose.yml's postgres service by default.
DATABASE_URL=postgresql://budget_app:budget_app@localhost:5432/budget_app?schema=public

# Comma-separated list of allowed browser origins for the (future) website client.
CORS_ORIGIN=http://localhost:19006

# Signs/verifies access JWTs (HS256). Must be at least 32 characters.
# Generate a real one with: openssl rand -base64 48
JWT_SECRET=changeme-generate-a-real-32-plus-character-secret

# Email provider API key (Resend/Postmark) is added when a real email
# provider replaces the console-log dev implementation.
